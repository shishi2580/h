name: 🚀 Build SukiSU Ultra for K20 Pro

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 下载 Proton 内核源码
      run: |
        echo "下载 Proton Kernel 作为基础..."
        git clone https://github.com/GZR-Kernels/Kernel-Proton-raphael.git --depth=1
        cd Kernel-Proton-raphael
        echo "基础内核大小:"
        du -sh .
        
    - name: 🔧 安装编译工具
      run: |
        sudo apt update
        sudo apt install -y \
          build-essential \
          gcc-aarch64-linux-gnu \
          gcc-10-aarch64-linux-gnu \
          libssl-dev \
          bc \
          flex \
          bison \
          libelf-dev \
          git \
          curl \
          python3
        
    - name: ⚡ 集成 SukiSU Ultra
      run: |
        cd Kernel-Proton-raphael
        
        echo "正在集成 SukiSU Ultra..."
        echo "这是专为 KSU 优化的内核"
        
        # 集成 SukiSU Ultra
        curl -LSs "https://raw.githubusercontent.com/SukiSU-Ultra/SukiSU-Ultra/main/kernel/setu
